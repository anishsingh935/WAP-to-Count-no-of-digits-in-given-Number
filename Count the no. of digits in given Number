#include <stdio.h>
#include<math.h>

int
main ()
{
  int num ,a;
  a == 0;
  printf ("Pls enter Some values to count:\n");
  scanf ("%d", &num);
  while (num != 0)
    {
      a++;
      num /= 10;
    }
    printf("Total digits In given No. is=%d",a);

  return 0;
}
